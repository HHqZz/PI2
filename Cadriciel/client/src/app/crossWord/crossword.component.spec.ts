import { CrossWordComponent } from "./crossword.component";

describe("CrossWordComponent", () => {
  it("should create the component", () => {
    const crossword: CrossWordComponent = new CrossWordComponent();

    expect(crossword).toBeTruthy();
  });
});
